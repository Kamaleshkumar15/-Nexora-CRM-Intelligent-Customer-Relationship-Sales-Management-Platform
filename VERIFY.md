# Verification

Run from this folder:

```powershell
npm install
npm run typecheck
npm run build
npm run dev
```

The project is intentionally frontend-first and stores demo mutations in browser localStorage while TanStack Query owns caching/invalidation.
