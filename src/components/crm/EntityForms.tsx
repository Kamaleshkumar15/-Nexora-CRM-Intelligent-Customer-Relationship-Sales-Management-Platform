"use client";
import { useState } from "react";
import { Button, Card, Input, Select, Textarea } from "@/components/ui";
import type { Company, Deal, Lead, Task } from "@/types/crm";
import type { Customer } from "@/types/customer";
export function LeadForm({customers,onSubmit,onCancel,loading=false}:{customers:Customer[];onSubmit:(x:Omit<Lead,"id">)=>void;onCancel:()=>void;loading?:boolean}){const c=customers[0];const[v,setV]=useState<Omit<Lead,"id">>({name:"",company:c?.company??"",email:c?.email??"",phone:c?.phone??"",stage:"New",temperature:"Warm",score:70,owner:c?.owner??"Kamal Kumar",source:"Website",customerId:c?.id});return <form className="space-y-4" onSubmit={e=>{e.preventDefault();if(v.name&&v.email)onSubmit(v)}}><div className="grid gap-3 md:grid-cols-2"><label className="text-xs text-slate-500">Name *<Input required className="mt-1" value={v.name} onChange={e=>setV({...v,name:e.target.value})}/></label><label className="text-xs text-slate-500">Email *<Input required type="email" className="mt-1" value={v.email} onChange={e=>setV({...v,email:e.target.value})}/></label><label className="text-xs text-slate-500">Phone<Input className="mt-1" value={v.phone} onChange={e=>setV({...v,phone:e.target.value})}/></label><label className="text-xs text-slate-500">Company<Input className="mt-1" value={v.company} onChange={e=>setV({...v,company:e.target.value})}/></label><label className="text-xs text-slate-500">Customer<Select className="mt-1 w-full" value={v.customerId??""} onChange={e=>{const x=customers.find(c=>c.id===e.target.value);setV({...v,customerId:x?.id,company:x?.company??v.company,email:x?.email??v.email,phone:x?.phone??v.phone})}}>{customers.slice(0,50).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</Select></label><label className="text-xs text-slate-500">Source<Select className="mt-1 w-full" value={v.source} onChange={e=>setV({...v,source:e.target.value})}><option>Website</option><option>Referral</option><option>Social</option><option>Campaign</option></Select></label></div><div className="grid gap-3 md:grid-cols-3"><label className="text-xs text-slate-500">Stage<Select className="mt-1 w-full" value={v.stage} onChange={e=>setV({...v,stage:e.target.value as Lead["stage"]})}><option>New</option><option>Contacted</option><option>Qualified</option><option>Proposal</option><option>Won</option></Select></label><label className="text-xs text-slate-500">Temperature<Select className="mt-1 w-full" value={v.temperature} onChange={e=>setV({...v,temperature:e.target.value as Lead["temperature"]})}><option>Hot</option><option>Warm</option><option>Cold</option></Select></label><label className="text-xs text-slate-500">Score<Input className="mt-1" type="number" min={0} max={100} value={v.score} onChange={e=>setV({...v,score:Number(e.target.value)})}/></label></div><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button disabled={loading} type="submit">{loading?"Saving…":"Create Lead"}</Button></div></form>}
export function DealForm({customers,onSubmit,onCancel,loading=false}:{customers:Customer[];onSubmit:(x:Omit<Deal,"id">)=>void;onCancel:()=>void;loading?:boolean}){const c=customers[0];const[v,setV]=useState<Omit<Deal,"id">>({name:"",customerId:c?.id??"",company:c?.company??"",value:50000,probability:60,stage:"Qualified",closeDate:new Date(Date.now()+14*86400000).toISOString().slice(0,10),owner:c?.owner??"Kamal Kumar"});return <form className="space-y-4" onSubmit={e=>{e.preventDefault();if(v.name&&v.customerId&&v.value>0)onSubmit(v)}}><div className="grid gap-3 md:grid-cols-2"><label className="text-xs text-slate-500">Deal Name *<Input required className="mt-1" value={v.name} onChange={e=>setV({...v,name:e.target.value})}/></label><label className="text-xs text-slate-500">Customer<Select className="mt-1 w-full" value={v.customerId} onChange={e=>{const x=customers.find(c=>c.id===e.target.value);setV({...v,customerId:e.target.value,company:x?.company??v.company,owner:x?.owner??v.owner})}}>{customers.slice(0,50).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</Select></label><label className="text-xs text-slate-500">Value *<Input required type="number" min={1} className="mt-1" value={v.value} onChange={e=>setV({...v,value:Number(e.target.value)})}/></label><label className="text-xs text-slate-500">Probability %<Input type="number" min={0} max={100} className="mt-1" value={v.probability} onChange={e=>setV({...v,probability:Number(e.target.value)})}/></label><label className="text-xs text-slate-500">Stage<Select className="mt-1 w-full" value={v.stage} onChange={e=>setV({...v,stage:e.target.value as Deal["stage"]})}><option>Qualified</option><option>Proposal</option><option>Negotiation</option><option>Won</option><option>Lost</option></Select></label><label className="text-xs text-slate-500">Expected Close<Input type="date" className="mt-1" value={v.closeDate} onChange={e=>setV({...v,closeDate:e.target.value})}/></label></div><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button disabled={loading} type="submit">{loading?"Saving…":"Create Deal"}</Button></div></form>}
export function TaskForm({customers,deals,onSubmit,onCancel,loading=false}:{customers:Customer[];deals:Deal[];onSubmit:(x:Omit<Task,"id">)=>void;onCancel:()=>void;loading?:boolean}){const c=customers[0];const[v,setV]=useState<Omit<Task,"id">>({title:"",customerId:c?.id,company:c?.company??"",dealId:deals[0]?.id,dueDate:new Date(Date.now()+86400000).toISOString().slice(0,10),priority:"Medium",status:"Todo",owner:c?.owner??"Kamal Kumar"});return <form className="space-y-4" onSubmit={e=>{e.preventDefault();if(v.title)onSubmit(v)}}><div className="grid gap-3 md:grid-cols-2"><label className="text-xs text-slate-500">Task *<Input required className="mt-1" value={v.title} onChange={e=>setV({...v,title:e.target.value})}/></label><label className="text-xs text-slate-500">Customer<Select className="mt-1 w-full" value={v.customerId??""} onChange={e=>{const x=customers.find(c=>c.id===e.target.value);setV({...v,customerId:e.target.value,company:x?.company??v.company,owner:x?.owner??v.owner})}}>{customers.slice(0,50).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</Select></label><label className="text-xs text-slate-500">Deal<Select className="mt-1 w-full" value={v.dealId??""} onChange={e=>setV({...v,dealId:e.target.value||undefined})}><option value="">No deal</option>{deals.slice(0,50).map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</Select></label><label className="text-xs text-slate-500">Due Date<Input type="date" className="mt-1" value={v.dueDate} onChange={e=>setV({...v,dueDate:e.target.value})}/></label><label className="text-xs text-slate-500">Priority<Select className="mt-1 w-full" value={v.priority} onChange={e=>setV({...v,priority:e.target.value as Task["priority"]})}><option>High</option><option>Medium</option><option>Low</option></Select></label><label className="text-xs text-slate-500">Status<Select className="mt-1 w-full" value={v.status} onChange={e=>setV({...v,status:e.target.value as Task["status"]})}><option>Todo</option><option>In Progress</option><option>Done</option></Select></label></div><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button disabled={loading} type="submit">{loading?"Saving…":"Create Task"}</Button></div></form>}
export function CompanyForm({onSubmit,onCancel,loading=false}:{onSubmit:(x:Omit<Company,"id">)=>void;onCancel:()=>void;loading?:boolean}){const[v,setV]=useState<Omit<Company,"id">>({name:"",industry:"Technology",city:"Chennai",customerIds:[],dealIds:[],taskIds:[],revenue:0,status:"Active"});return <form className="space-y-4" onSubmit={e=>{e.preventDefault();if(v.name)onSubmit(v)}}><div className="grid gap-3 md:grid-cols-2"><label className="text-xs text-slate-500">Company Name *<Input required className="mt-1" value={v.name} onChange={e=>setV({...v,name:e.target.value})}/></label><label className="text-xs text-slate-500">Industry<Select className="mt-1 w-full" value={v.industry} onChange={e=>setV({...v,industry:e.target.value})}><option>Technology</option><option>Finance</option><option>Healthcare</option><option>Retail</option><option>Education</option><option>Manufacturing</option></Select></label><label className="text-xs text-slate-500">City<Input className="mt-1" value={v.city} onChange={e=>setV({...v,city:e.target.value})}/></label><label className="text-xs text-slate-500">Status<Select className="mt-1 w-full" value={v.status} onChange={e=>setV({...v,status:e.target.value as Company["status"]})}><option>Active</option><option>Prospect</option></Select></label></div><Card className="p-3 text-xs text-slate-500">New companies start with zero relationships. Link customers, deals and tasks from their connected workspaces.</Card><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button disabled={loading} type="submit">{loading?"Saving…":"Create Company"}</Button></div></form>}
export function NoteForm({onSubmit,onCancel}:{onSubmit:(text:string)=>void;onCancel:()=>void}){const[text,setText]=useState("");return <form className="space-y-4" onSubmit={e=>{e.preventDefault();if(text.trim())onSubmit(text.trim())}}><label className="text-xs text-slate-500">Interaction Note<Textarea autoFocus className="mt-1 min-h-32" value={text} onChange={e=>setText(e.target.value)} placeholder="Write a customer interaction, observation or follow-up note…"/></label><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button type="submit">Save Note</Button></div></form>}
